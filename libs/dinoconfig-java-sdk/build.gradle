/*
 * DinoConfig Java SDK - Root Build Configuration
 */

subprojects {
    repositories {
        mavenCentral()
    }
    
    afterEvaluate {
        tasks.withType(JavaCompile).configureEach {
            options.encoding = 'UTF-8'
        }
    }
}
